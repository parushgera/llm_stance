# Pinned dependencies for the *SEM 2026 / ACL 2026 paper:
#   "Diagnosing Generalization in Open-Source LLMs for Stance Detection"
#
# Tested with Python 3.10 on NVIDIA H100 GPUs (CUDA 12.1).
# `pip install -r requirements.txt` followed by a CUDA-matched torch build
# is the recommended setup; see README.md.

# --- core scientific stack ---
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
matplotlib==3.9.2
seaborn==0.13.2
tqdm==4.66.5

# --- HuggingFace inference + PEFT/LoRA fine-tuning ---
torch==2.3.1
transformers==4.44.2
peft==0.12.0
accelerate==0.33.0
datasets==2.20.0
huggingface_hub==0.24.6
bitsandbytes==0.43.3   # optional, for 4/8-bit loading on smaller GPUs
sentencepiece==0.2.0
safetensors==0.4.4
tokenizers==0.19.1

# --- notebooks / development ---
jupyter==1.0.0
ipykernel==6.29.5
